/**
 * Local barrel for the Sellsuki Design System package.
 *
 * This gives the codebase one stable import path for all DS React components:
 *   import { DSButton, StatCard, Alert } from "@/components/ui/sellsuki-ds"
 *
 * Source package:
 *   @uxuissk/design-system
 */

export * from "@uxuissk/design-system";
